<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>API Dashboard</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f4f6f9; margin: 0; padding: 20px; color: #333; }
    .container { max-width: 900px; margin: 0 auto; background: #fff; padding: 25px; border-radius: 10px; 
      box-shadow: 0 4px 10px rgba(0,0,0,0.1); }
    h1 { text-align: center; color: #0077cc; }
    h2 { margin-top: 30px; color: #444; }
    .route-box { background: #eef5ff; padding: 12px; margin: 8px 0; border-left: 5px solid #0077cc; 
      border-radius: 5px; font-size: 16px; }
    .link { color: #0077cc; text-decoration: none; }
    .link:hover { text-decoration: underline; }
    .footer { margin-top: 40px; text-align: center; color: #777; font-size: 14px; }
  </style>
</head>
<body>
  <div class="container">
    <h1>🚀 CSE40 Week 06 API Dashboard</h1>
    <p>Welcome! Your backend is running successfully on Render.</p>

    <h2>✔ API Status</h2>
    <p><strong>Status:</strong> <%= status %></p>
    <p><strong>Message:</strong> <%= message %></p>

    <h2>📌 API Endpoints (JSON only)</h2>

    <% const apiList = [
      { method: "POST", path: "/api/orders" },
      { method: "GET", path: "/api/orders/myorders" },
      { method: "PUT", path: "/api/orders/:id/status" },
      { method: "GET", path: "/api/products" },
      { method: "GET", path: "/api/reviews" },
      { method: "GET", path: "/api/wishlist" },
      { method: "POST", path: "/api/users/login" },
      { method: "POST", path: "/api/users/register" }
    ]; %>

    <% apiList.forEach(api => { %>
      <div class="route-box">
        <strong><%= api.method %></strong> — <%= api.path %>
      </div>
    <% }) %>

    <h2>📌 Pages (HTML views)</h2>

    <div class="route-box"><a class="link" href="/products">Products Page</a></div>
    <div class="route-box"><a class="link" href="/wishlist">Wishlist Page</a></div>
    <div class="route-box"><a class="link" href="/reviews">Reviews Page</a></div>
    <div class="route-box"><a class="link" href="/orders">Orders Page</a></div>

    <div class="footer">
      <p>© <%= new Date().getFullYear() %> CSE40 — Designed by Reagan Otema</p>
    </div>
  </div>
</body>
</html>
