<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Error - CSE40 Application</title>
  <style>
    body { font-family: Arial, sans-serif; background:#fff4f4; color:#721c24; margin:0; padding:30px; }
    .container { max-width:900px; margin:30px auto; background:#fff; border-left:6px solid #c0392b; padding:20px; border-radius:8px; box-shadow:0 4px 10px rgba(0,0,0,0.05); }
    h1 { margin-top:0; color:#c0392b; }
    pre { background:#f7f7f7; padding:12px; border-radius:6px; overflow:auto; }
    a { color:#0077cc; text-decoration:none; }
    footer { margin-top:20px; color:#666; font-size:14px; text-align:center; }
  </style>
</head>
<body>
  <div class="container">
    <h1>❌ An Error Occurred</h1>

    <p><strong>Message:</strong> <%= typeof message !== "undefined" ? message : (error && error.message ? error.message : "Unknown error") %></p>

    <% if (typeof error !== "undefined" && error && error.stack) { %>
      <h3>Stack trace (development only)</h3>
      <pre><%= error.stack %></pre>
    <% } %>

    <p><a href="/">← Back to Dashboard</a></p>
  </div>

  <footer>
    © <%= new Date().getFullYear() %> CSE40 — Designed by Reagan Otema
  </footer>
</body>
</html>
