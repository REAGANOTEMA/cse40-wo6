<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Error</title>

  <style>
    body {
      font-family: Arial, sans-serif;
      background: #ffecec;
      padding: 40px;
      color: #a10000;
    }

    .error-box {
      background: #fff4f4;
      padding: 25px;
      border-left: 7px solid #ff3b3b;
      border-radius: 6px;
      max-width: 700px;
      margin: 0 auto;
    }

    h1 {
      margin-top: 0;
    }

    .footer {
      margin-top: 40px;
      text-align: center;
      color: #777;
      font-size: 14px;
    }
  </style>
</head>

<body>

  <div class="error-box">
    <h1>❌ An Error Occurred</h1>

    <p><strong>Message:</strong> <%= error && error.message ? error.message : "Unknown error" %></p>

    <% if (error && error.stack) { %>
      <pre><%= error.stack %></pre>
    <% } %>
  </div>

  <div class="footer">
    <p>© <%= new Date().getFullYear() %> CSE40 API – Error Handler</p>
  </div>

</body>
</html>
